// RISE FROM BROKEN configuration
// IMPORTANT: never put your Supabase service_role key here.
// The anon/public key is safe for browser use when your database RLS policies are configured.
//
// After creating your Supabase table, put your values below or set them in Vercel.
// For the first demo, you can leave these blank: RISE FROM BROKEN will use demo data.

window.RISE FROM BROKEN_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
  SEARCH_TABLE: "search_documents"
};
